# QA Checklist

- Verify `/`, `/trail`, `/rustys-route-1000`, `/resources`, `/news`, `/contact`, `/shop`, `/cart`, `/login`, `/privacy`, and `/studio`.
- Confirm old WordPress redirects resolve.
- Check keyboard navigation and visible focus states.
- Test the interactive map layer controls.
- Test contact form success and error behavior.
- Run Lighthouse on home, trail, route, news, and contact pages.
- Verify reduced-motion mode does not depend on animation.
- Confirm mobile width has no horizontal overflow.
